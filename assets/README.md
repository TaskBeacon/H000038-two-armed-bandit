# Assets

This folder is intentionally empty for the browser companion.

The canonical Python task includes an instruction voice MP3, but the web port keeps `voice_enabled: false` and uses text-only instructions by default. If the task is reconfigured to use browser speech synthesis later, no extra per-task asset is required.
